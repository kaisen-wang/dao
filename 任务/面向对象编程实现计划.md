# 面向对象编程实现计划

基于 `文档/03-面向对象编程.md` 设计文档与实际代码的对照检查，梳理出以下实现状态和待完成任务。

## 总览

| 状态 | 数量 |
|------|------|
| ✅ 已完成 | 9 |
| ❌ 未实现 | 1 |
| **总计** | **10** |

---

## 已完成特性

### 3.1 类定义（类型关键字） ✅

- **实现位置**：`tokens.py` TokenType.类型；`ast_nodes.py` ClassDecl；`parser/modules/oop_decl.py` parse_class_decl()；`interpreter/statements.py` exec_class_decl()
- **测试**：test_new_features.py TestOOPBasics（6个测试）

### 3.2 继承（继承自）与方法重写 ✅

- **实现位置**：`tokens.py` TokenType.继承自；`ast_nodes.py` ClassDecl.parent_name；`parser/modules/oop_decl.py`；`interpreter/statements.py` exec_class_decl()；`builtins/oop_types.py` DaoClass.find_method()
- **测试**：test_new_features.py test_inheritance, test_polymorphism, test_inherited_method

### 3.3 特征（Trait/Interface） ✅

- **实现位置**：`tokens.py` TokenType.特征/实现；`ast_nodes.py` TraitDecl, ClassDecl.implemented_traits；`parser/modules/oop_decl.py` parse_trait_decl()；`interpreter/statements.py` exec_trait_decl()；`builtins/oop_types.py` DaoTrait
- **测试**：test_traits.py（8个测试）

### 3.4 抽象类型 ✅

- **实现位置**：`tokens.py` TokenType.抽象；`ast_nodes.py` AbstractDecl, FunctionDecl.is_abstract；`parser/modules/oop_decl.py` parse_abstract_decl()；`interpreter/statements.py` exec_abstract_decl()
- **测试**：test_abstract_type.py（11个测试）

### 3.5 静态方法 ✅

- **实现位置**：`tokens.py` TokenType.静态；`ast_nodes.py` FunctionDecl.is_static；`parser/modules/oop_decl.py`；`builtins/oop_types.py` DaoClass.static_methods, find_static_method()
- **测试**：test_new_features.py TestStaticMethods（3个测试）

### 3.6 运算符重载 ✅

- **实现位置**：`tokens.py` TokenType.运算符；`ast_nodes.py` FunctionDecl.is_operator/operator_symbol；`parser/modules/function_decl.py` _parse_operator_overload()；`interpreter/expressions.py` eval_binary_op()
- **测试**：test_operator_overload.py（15个测试）

### 3.7 属性访问器（获取/设置） ✅

- **实现位置**：`tokens.py` TokenType.获取/设置；`ast_nodes.py` FunctionDecl.is_getter/is_setter；`parser/modules/function_decl.py` _parse_property_accessor()；`interpreter/expressions.py` eval_member_access()；`interpreter/statements.py` exec_assignment()
- **测试**：test_property_accessor.py（9个测试）

### 3.8 私有访问控制 ✅

- **实现位置**：
  - `tokens.py` TokenType.私有
  - `ast_nodes.py` FunctionDecl.is_private, Assignment.is_private
  - `parser/statements.py` 解析 `私有` 修饰赋值语句
  - `parser/modules/oop_decl.py` 类体中 `私有` 修饰方法
  - `builtins/oop_types.py` DaoClass.private_names, DaoInstance.private_fields
  - `interpreter/expressions.py` eval_member_access() 私有方法+字段读取检查
  - `interpreter/statements.py` exec_assignment() 私有字段标记+写入检查
  - `interpreter/core.py` _in_method_context() 判断是否在同类方法内
- **测试**：test_private_field.py（7个测试），test_new_features.py TestAccessControl（2个测试）

### 3.9 受保护访问控制 ✅

- **实现位置**：
  - `tokens.py` TokenType.受保护
  - `ast_nodes.py` FunctionDecl.is_protected, Assignment.is_protected
  - `parser/statements.py` 解析 `受保护` 修饰赋值语句
  - `parser/modules/oop_decl.py` 类体中 `受保护` 修饰方法和属性访问器
  - `builtins/oop_types.py` DaoClass.protected_names, DaoInstance.protected_fields
  - `interpreter/expressions.py` eval_member_access() 受保护方法+字段读取检查
  - `interpreter/statements.py` exec_assignment() 受保护字段标记+写入检查, exec_class_decl() 收集 protected_names
  - `interpreter/core.py` _in_protected_context() 判断是否在本类或子类方法内
- **测试**：test_protected_access.py（7个测试）

---

## 代码质量问题（已修复）

### Q.1 清理调试残留 ✅

- 移除了 `interpreter/core.py` 中 `_instantiate_class` 的 `print` 调试语句

### Q.2 修复重复异常处理 ✅

- 合并了 `interpreter/core.py` 中 `_call_method` 的重复 `except Exception` 块

---

## 未实现特性

### 3.10 枚举类型增强 ❌

- **当前状态**：枚举基础已实现（EnumDecl/DaoEnum），但与 OOP 的深度集成（如枚举实现特征、枚举方法）尚未支持。
- **优先级**：低

---

## 验收标准

- [x] `tokens.py` / `ast_nodes.py` / `parser/` / `interpreter/` / `builtins/` 同步更新
- [x] 每个新特性至少 3 个测试用例
- [x] `ruff check dao/ tests/` 无新增错误
- [x] 现有测试不受影响（`pytest tests/ -v` 488 passed, 2 failed 为宏系统已有问题）
