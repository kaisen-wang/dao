# 宏系统未实现功能

> 基于 Phase 3 详细开发计划与代码实际对比，完成度约 95%

## 1. 内置宏未实现

**位置**：`dao/macros/expander.py` — `register_builtin_macros()` 方法

**现状**：方法体为空（`pass`），未注册任何内置宏

**计划中的内置宏**：
- [ ] `@除非` — 条件取反宏（`除非 条件` → `如果 不是 条件`）
- [ ] `@计时` — 执行计时宏（自动包裹计时逻辑）
- [ ] `@重试` — 重试宏（失败时自动重试指定次数）
- [ ] `@日志` — 日志宏（自动添加函数进入/退出日志）
- [ ] `@缓存` — 缓存宏（函数结果缓存）
- [ ] `@延迟` — 延迟计算宏

**任务**：
- [ ] 在 `register_builtin_macros()` 中注册常用内置宏
- [ ] 每个内置宏需有对应的测试用例
- [ ] 在文档中说明内置宏的用法

---

## 2. 卫生宏捕获检测较粗糙

**位置**：`dao/macros/hygiene.py` — `HygieneAnalysis.get_capture_risk()`

**现状**：仅检查 `used_lines > 1` 判断变量捕获风险

**问题**：
- 可能误报：多行使用同一变量不一定有捕获风险
- 可能漏报：跨作用域的隐式捕获未检测

**任务**：
- [ ] 改进捕获检测算法，基于作用域链分析
- [ ] 区分自由变量和绑定变量的捕获风险
- [ ] 添加捕获风险等级（高/中/低）
- [ ] 提供捕获风险报告

---

## 3. 穷尽性检查器简单

**位置**：`dao/macros/exhaustiveness.py` — `ExhaustivenessChecker`

**现状**：仅检查最后一个分支是否为通配符/变量绑定

**缺失**：
- [ ] 枚举类型的穷尽性检查（所有变体是否覆盖）
- [ ] 布尔类型的穷尽性检查（真/假是否都覆盖）
- [ ] 守卫条件对穷尽性的影响分析
- [ ] 缺失分支提示（建议用户补充哪些分支）

**任务**：
- [ ] 实现基于类型的穷尽性分析
- [ ] 支持枚举变体的穷尽性检查
- [ ] 生成缺失分支的提示信息
- [ ] 添加穷尽性检查的测试用例

---

## 4. 枚举变体模式耦合度高

**位置**：`dao/macros/pattern_engine.py` — `_match_enum_variant_pattern()`

**现状**：依赖运行时对象的 `__dao_class__` 和 `__variant__` 属性

**问题**：
- 与 OOP 实现细节强耦合
- 如果 OOP 模块修改属性名，宏系统也会受影响
- 无法匹配非道语言创建的对象

**任务**：
- [ ] 定义统一的模式匹配协议接口
- [ ] 解耦枚举变体匹配与 OOP 内部实现
- [ ] 支持自定义对象的模式匹配

---

## 5. 宏展开错误恢复不完善

**位置**：`dao/macros/expander.py`

**现状**：宏展开出错时直接抛出异常，中断整个展开过程

**缺失**：
- [ ] 部分展开支持（跳过出错的宏，继续展开其他宏）
- [ ] 展开错误的位置信息（指向源代码中的宏调用位置）
- [ ] 错误恢复策略配置（严格模式/宽松模式）

**任务**：
- [ ] 实现宏展开错误的位置追踪
- [ ] 支持部分展开和错误恢复
- [ ] 提供清晰的错误信息（包含源代码位置）

---

## 6. 宏调试支持不足

**计划任务**（M3.20）：调试支持

**缺失**：
- [ ] 显示宏展开前后的 AST 对比
- [ ] 宏展开步骤追踪
- [ ] 宏调用栈显示
- [ ] 展开结果的可视化

**任务**：
- [ ] 实现 `set_trace(True)` 的详细追踪输出
- [ ] 支持展开前后 AST 的 diff 显示
- [ ] 添加宏调试的 REPL 命令

---

## 7. 缺少示例程序

**计划中的示例**（部分未实现）：
- [ ] `除非宏.道`
- [ ] `计时宏.道`
- [ ] `重试宏.道`
- [ ] `日志宏.道`
- [ ] `HTML构建器.道`

---

## 8. 性能目标未验证

- [ ] 宏展开 < 1ms
- [ ] 创建宏系统性能基准测试
