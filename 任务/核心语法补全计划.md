# 核心语法补全计划

基于 `文档/02-核心语法.md` 设计文档与实际代码的对照检查，梳理出以下未实现和部分实现的功能，按优先级和依赖关系分批推进。

## 总览

| 状态 | 数量 |
|------|------|
| ✅ 已完成 | 15 |
| ⚠️ 部分实现 | 0 |
| ❌ 未实现 | 0 |
| **全部完成** | **15/15** |

---

## 第一批：基础数据类型补全（优先级：高）

这批任务为后续功能（解构、多返回值等）提供基础支撑。

### 任务 1.1：元组字面量

- **状态**：✅ 已完成
- **实现位置**：`ast_nodes.py` TupleLiteral；`parser/expressions.py` 括号内逗号解析（第527-557行）；`interpreter/expressions.py` eval 返回 Python tuple

### 任务 1.2：集合字面量

- **状态**：✅ 已完成
- **实现位置**：`ast_nodes.py` SetLiteral；`parser/expressions.py` `parse_set_or_block_literal()`；`interpreter/expressions.py` eval 返回 Python set

### 任务 1.3：带关联数据的枚举变体

- **状态**：✅ 已完成
- **实现位置**：`ast_nodes.py` EnumDecl/EnumVariant；`parser/modules/oop_decl.py` `parse_enum_decl()`；`interpreter/statements.py` DaoEnum 类、`exec_enum_decl()`

### 任务 1.4：文档注释 `///`

- **状态**：✅ 已完成
- **实现位置**：`tokens.py` TokenType.文档注释；`lexer/readers.py` 第30-43行区分 `///` 与 `//`，生成文档注释 Token

---

## 第二批：解构赋值补全（优先级：高）

这批任务依赖第一批的元组字面量。

### 任务 2.1：字典解构

- **状态**：✅ 已完成
- **实现位置**：`parser/modules/variable_decl.py` `_parse_dict_destructure_decl()`；`interpreter/statements.py` `exec_destructure()` 中 `is_dict_destructure` 分支

### 任务 2.2：嵌套解构

- **状态**：✅ 已完成
- **实现位置**：`ast_nodes.py` DestructureTarget 树形节点；`parser/modules/variable_decl.py` `_parse_list_destructure_target()`/`_parse_dict_destructure_target()`/`_parse_tuple_destructure_target()` 递归解析；`interpreter/statements.py` `_exec_destructure_pattern()` 递归执行
- **测试**：`tests/test_nested_destructure.py`（10 个测试用例）

### 任务 2.3：解构赋值中的剩余元素

- **状态**：✅ 已完成
- **实现位置**：`parser/modules/variable_decl.py` 第71-75行 TokenType.展开；`ast_nodes.py` DestructureAssign.rest_target；`interpreter/statements.py` `exec_destructure()` 第994-1020行

### 任务 2.4：函数参数解构

- **状态**：✅ 已完成
- **实现位置**：`parser/modules/function_decl.py` `_parse_param_list()` 支持解构参数；`interpreter/core.py` `_bind_params` 支持解构绑定

---

## 第三批：控制流与函数补全（优先级：中）

### 任务 3.1：条件表达式（三元）

- **状态**：✅ 已完成
- **实现位置**：`ast_nodes.py` ConditionalExpr；`parser/expressions.py` `parse_conditional()` 实现 `值 如果 条件 否则 值`；`interpreter/expressions.py` `eval_conditional()`

### 任务 3.2：遍历字典双变量

- **状态**：✅ 已完成
- **实现位置**：`parser/modules/control_flow.py` 第90-104行 `遍历 键, 值 在 字典`；`ast_nodes.py` ForInStmt.second_variable；`interpreter/statements.py` `exec_for_in()` 第351-361行

### 任务 3.3：可变参数

- **状态**：✅ 已完成
- **实现位置**：`parser/modules/function_decl.py` `_parse_param_list()` 第186-189行 TokenType.展开；`ast_nodes.py` FunctionDecl.rest_param；`builtins/callables.py` DaoFunction.rest_param；`interpreter/core.py` `_bind_params()` 第361-376行

### 任务 3.4：多返回值（元组解构接收）

- **状态**：✅ 已完成
- **实现位置**：`parser/modules/function_decl.py` `parse_return_stmt()` 支持 `返回 甲, 乙` 语法糖（逗号分隔自动构造 TupleLiteral）；`parser/modules/variable_decl.py` `_parse_tuple_destructure_decl()` 支持 `定义 (甲, 乙) = ...` 元组解构；`lexer/core.py` 修复 `//` 整除运算符在标识符后的上下文判断
- **测试**：`tests/test_multi_return.py`（5 个测试用例）

---

## 第四批：运算符补全（优先级：中低）

### 任务 4.1：整除运算符 `//`

- **状态**：✅ 已完成
- **实现位置**：`tokens.py` TokenType.整除；`lexer/core.py` 第164-194行上下文判断整除/注释；`parser/expressions.py` parse_addition；`interpreter/expressions.py` eval_binary_op case "//"

### 任务 4.2：逻辑运算符 `&&`/`||`/`!`

- **状态**：✅ 已完成
- **实现位置**：`lexer/readers.py` 第433-436行 `&&`→TokenType.并且，第437-440行 `||`→TokenType.或者，第453行 `!`→TokenType.感叹号；`parser/expressions.py` parse_not 第128-139行处理 `!` 作为逻辑非

---

## 第五批：字符串增强（优先级：低）

### 任务 5.1：多行字符串（三引号语法）

- **状态**：✅ 已完成
- **实现位置**：`lexer/readers.py` `_read_triple_quoted_string()` 第119-157行；`lexer/core.py` 第210-212行三引号检测
- **测试**：`tests/test_triple_quote.py`

---

## 执行顺序与依赖关系

```
第一批（基础类型）    第二批（解构）       第三批（控制流/函数）   第四批（运算符）   第五批（字符串）
─────────────────    ──────────────      ──────────────────    ──────────────    ──────────────
1.1 元组字面量 ✅    2.4 函数参数解构 ✅  3.1 条件表达式 ✅      4.1 整除 // ✅    5.1 三引号字符串 ✅
1.2 集合字面量 ✅    2.1 字典解构 ✅      3.2 遍历双变量 ✅      4.2 &&/||/! ✅
1.3 枚举变体 ✅     2.2 嵌套解构 ✅      3.3 可变参数 ✅
1.4 文档注释 ✅     2.3 剩余元素 ✅      3.4 多返回值 ✅
```

**全部 15 项任务已完成！**

---

## 每个任务的验收标准

- [x] `tokens.py` / `ast_nodes.py` / `lexer/` / `parser/` / `interpreter/` 同步更新
- [x] 至少 3 个测试用例通过
- [x] `ruff check dao/ tests/` 无新增错误
- [x] 现有测试不受影响（`pytest tests/ -v` 474 passed, 2 failed 均为宏系统已有问题）