# 模块系统实现计划

基于 `文档/07-模块系统.md` 的需求对照现有实现，梳理待完成项。

## 实现状态总览

| 章节 | 功能 | 状态 | 说明 |
|------|------|------|------|
| 7.2.1 | 逐项导出（`导出 函数 加`） | ✅ 已实现 | 在定义前加 `导出` 修饰，设置 `is_exported` 标记 |
| 7.2.2 | 集中导出（`导出 大写, 小写, 修剪`） | ✅ 已实现 | 逗号分隔语法 |
| 7.3.1 | 导入整个模块 | ✅ 已实现 | `导入 模块名` |
| 7.3.2 | 导入特定项 | ✅ 已实现 | `从 模块 导入 项1, 项2`（逗号分隔） |
| 7.3.3 | 重命名导入 | ✅ 已实现 | `从 模块 导入 名称 作为 别名` |
| 7.3.3 | 导入整个模块重命名 | ✅ 已实现 | `导入 模块 作为 别名` |
| 7.3.4 | 导入子目录模块 | ✅ 已实现 | 点分路径解析 + 搜索路径列表 |
| 7.4 | 包管理（道.配置） | ❌ 未实现 | 包定义文件、依赖管理、第三方包导入（远期规划） |
| 7.5 | 模块初始化与缓存 | ✅ 已实现 | `module_cache` 字典，首次导入执行并缓存 |
| 7.6 | 条件导入 | ✅ 已验证 | `如果/否则` + `导入` 组合即可实现 |
| - | 循环依赖检测 | ✅ 已实现 | `_loading_modules` 集合检测循环导入 |
| - | 标准库/内置模块路径搜索 | ✅ 已实现 | `module_search_paths` 列表，按路径依次查找 |

## 已完成任务

### ~~任务1：逐项导出语法（7.2.1）~~ ✅

- `ast_nodes.py`：`VariableDecl`、`FunctionDecl`、`ClassDecl`、`EnumDecl`、`TraitDecl`、`AbstractDecl` 添加 `is_exported` 字段
- `parser/statements.py`：`导出` 后跟定义关键字时，解析定义并设置 `is_exported`
- `interpreter/statements.py`：各 `exec_*` 方法中，`is_exported` 时自动将名称加入 `env.exports`

### ~~任务2：集中导出逗号语法（7.2.2）~~ ✅

- `parser/modules/module_system.py`：移除花括号语法，改为逗号分隔

### ~~任务3：重命名导入（7.3.3）~~ ✅

- `ast_nodes.py`：`ImportStmt.names` 改为 `list[tuple[str, str | None]]`，支持 `(名称, 别名)` 元组
- `parser/modules/module_system.py`：`parse_import_stmt` 的 from_import 分支解析 `作为`
- `interpreter/statements.py`：`exec_import` 中处理别名映射

### ~~任务4：模块缓存（7.5）~~ ✅

- `interpreter/core.py`：添加 `module_cache: dict[str, Environment]`
- `interpreter/statements.py`：`exec_import` 先查缓存，命中则跳过解析执行

### ~~任务5：嵌套模块导入完善（7.3.4）~~ ✅

- `tests/module_system/test_import_export.py`：实现 `test_nested_module_import`，添加 `run_with_modules` 辅助函数

### ~~任务6：循环依赖检测~~ ✅

- `interpreter/core.py`：添加 `_loading_modules: set[str]`
- `interpreter/statements.py`：`exec_import` 中检测循环导入并抛出友好错误

### ~~任务7：标准库/内置模块路径搜索~~ ✅

- `interpreter/core.py`：添加 `module_search_paths: list[str]`
- `interpreter/statements.py`：`exec_import` 按路径列表依次查找模块

### ~~任务8：条件导入（7.6）~~ ✅

- 验证 `如果/否则` + `导入` 组合可正常工作

## 未实现任务

### 任务9：包管理系统（7.4）

**优先级**：低（远期）

**需求**：`道.配置` 文件解析、依赖声明、第三方包导入。

**改动范围**：
- 新建 `dao/package/` 模块
- 解析 `道.配置` 文件
- 依赖版本解析
- 包下载与安装

**测试用例**：
- 配置文件解析
- 依赖声明与版本约束
- 第三方包导入
