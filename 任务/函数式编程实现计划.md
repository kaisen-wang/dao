# 函数式编程实现计划

基于 `文档/04-函数式编程.md` 设计文档与实际代码的对照检查，梳理出以下实现状态和待完成任务。

## 总览

| 状态 | 数量 |
|------|------|
| ✅ 已完成 | 5 |
| ⚠️ 部分实现 | 4 |
| ❌ 未实现 | 0 |
| **总计** | **9** |

---

## 已完成特性

### 4.1 一等公民函数 ✅

- **实现位置**：`builtins/callables.py` DaoFunction（closure_env）；`interpreter/statements.py` exec_function_decl()；`interpreter/expressions.py` eval_lambda()
- **测试**：test_interpreter.py test_closure；test_new_features.py test_oop_with_functional

### 4.3 高阶函数 ✅

- **实现位置**：`builtins/hof.py` — 映射(62行)、筛选(67行)、折叠(73行)、展平映射(116行)、每个满足(81行)、存在满足(89行)、查找(97行)、分组(128行)、排序(105行)
- **测试**：test_new_features.py TestHigherOrderFunctions（8个测试）
- **待补充**：`分组` 函数无测试用例

### 4.4 管道运算符 ✅

- **实现位置**：`tokens.py` TokenType.管道；`lexer/readers.py` 第409行；`ast_nodes.py` PipeExpr；`parser/expressions.py` parse_pipe()；`interpreter/expressions.py` eval_pipe()
- **测试**：test_new_features.py TestPipeOperator（5个测试）

### 4.5 柯里化 ✅

- **实现位置**：`builtins/hof.py` _hof_柯里化()；`builtins/callables.py` CurriedFunction（第119行）
- **测试**：test_new_features.py TestCurrying（5个测试）

### 4.6 函数组合 ✅

- **实现位置**：`builtins/hof.py` _hof_组合()、_hof_管道组合()
- **测试**：test_new_features.py TestFunctionComposition（5个测试）

---

## 部分实现特性

### 4.2 匿名函数（Lambda） ⚠️

**已实现**：
- 箭头语法 `函数(x) => 表达式` 和无参数 `函数 => 表达式`
- 实现位置：`parser/expressions.py` parse_lambda()；`interpreter/expressions.py` eval_lambda()

**未实现**：
- [ ] Lambda 代码块语法：`函数(x) => 语句 ` 使用缩进块形式
- [ ] 修改 `parse_lambda()` 支持解析代码块体（当前仅解析单表达式）
- [ ] 修改 `eval_lambda()` 处理代码块体形式的 body
- [ ] 添加测试用例

### 4.7 模式匹配 ⚠️

**已实现**：
- 基础匹配（值/通配符）、列表解构匹配、字典解构匹配、守卫条件 `当`
- 实现位置：`interpreter/statements.py` exec_match()、_match_pattern()；`parser/modules/pattern_matching.py`
- **测试**：test_pattern_matching.py；test_new_features.py TestPatternMatching

**未实现**：
- [ ] 枚举匹配：`_match_pattern()` 中添加 `EnumVariantPattern` 处理分支（AST节点和解析已在宏系统中存在）
- [ ] 类型检查模式：`_match_pattern()` 中添加 `TypeCheckPattern` 处理分支
- [ ] 添加枚举匹配和类型检查模式的测试用例

### 4.8 惰性求值 ⚠️

**已实现**：
- `产出` 关键字和生成器函数（DaoGenerator）
- 实现位置：`tokens.py` TokenType.产出；`ast_nodes.py` YieldStmt；`interpreter/statements.py` exec_yield()；`builtins/oop_types.py` DaoGenerator
- **测试**：test_generator.py；test_new_features.py TestGenerators

**未实现**：
- [ ] `流`(Stream) 惰性数据结构：创建 Stream 类，按需计算而非预生成
- [ ] `映射流` 内置函数：对流惰性映射
- [ ] `筛选流` 内置函数：对流惰性筛选
- [ ] `取` 内置函数：从流中取前 N 个值
- [ ] 重构 DaoGenerator 为真正的惰性求值模式（当前为预生成模式 `_collect_all_values`）
- [ ] 添加流相关测试用例

### 4.9 不可变数据操作 ⚠️

**已实现**：
- 模式匹配中的展开 `...`（收集剩余元素语义）
- 解构赋值中的 rest
- 实现位置：`ast_nodes.py` ListPattern.has_spread、DestructureTarget.list_rest；`interpreter/statements.py` _match_pattern()、exec_destructure()

**未实现**：
- [ ] 字典字面量中的展开：`{...dict1, "新键": 值}` — 修改 `parse_dict_literal()` 和字典求值逻辑
- [ ] 列表字面量中的展开：`[...list1, 新元素]` — 修改 `parse_list_literal()` 和列表求值逻辑
- [ ] 添加展开运算符在字面量构造中的测试用例

---

## 优先级建议

| 优先级 | 任务 | 难度 | 影响范围 |
|--------|------|------|----------|
| P0 | 不可变数据操作 - 字典/列表字面量展开 | 中 | parser, interpreter |
| P0 | Lambda 代码块语法 | 中 | parser, interpreter |
| P1 | 模式匹配 - 枚举匹配支持 | 低 | interpreter/statements.py |
| P1 | 惰性求值 - 流数据结构 | 高 | 新增模块, builtins |
| P2 | 分组函数测试补充 | 低 | tests |
| P2 | 模式匹配 - 类型检查模式 | 低 | interpreter/statements.py |