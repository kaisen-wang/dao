# 函数式编程实现计划

基于 `文档/04-函数式编程.md` 设计文档与实际代码的对照检查，梳理出以下实现状态和待完成任务。

## 总览

| 状态 | 数量 |
|------|------|
| ✅ 已完成 | 9 |
| ⚠️ 部分实现 | 0 |
| ❌ 未实现 | 0 |
| **总计** | **9** |

---

## 已完成特性

### 4.1 一等公民函数 ✅

- **实现位置**：`builtins/callables.py` DaoFunction（closure_env）；`interpreter/statements.py` exec_function_decl()；`interpreter/expressions.py` eval_lambda()
- **测试**：test_interpreter.py test_closure；test_new_features.py test_oop_with_functional

### 4.2 匿名函数（Lambda） ✅

- **箭头语法**：`函数(x) => 表达式` 和无参数 `函数 => 表达式`
- **代码块语法**：`函数(x) => 缩进块` — 修改 `parse_lambda()` 检测 `=>` 后换行，调用 `parse_block()`
- **实现位置**：`parser/expressions.py` parse_lambda()；`interpreter/expressions.py` eval_lambda()
- **测试**：test_interpreter.py test_lambda, test_lambda_block_body, test_lambda_block_body_no_args, test_lambda_block_closure；test_parser.py test_lambda, test_lambda_block_body

### 4.3 高阶函数 ✅

- **实现位置**：`builtins/hof.py` — 映射(62行)、筛选(67行)、折叠(73行)、展平映射(116行)、每个满足(81行)、存在满足(89行)、查找(97行)、分组(128行)、排序(105行)
- **测试**：test_new_features.py TestHigherOrderFunctions（9个测试，含分组测试）

### 4.4 管道运算符 ✅

- **实现位置**：`tokens.py` TokenType.管道；`lexer/readers.py` 第409行；`ast_nodes.py` PipeExpr；`parser/expressions.py` parse_pipe()；`interpreter/expressions.py` eval_pipe()
- **测试**：test_new_features.py TestPipeOperator（5个测试）

### 4.5 柯里化 ✅

- **实现位置**：`builtins/hof.py` _hof_柯里化()；`builtins/callables.py` CurriedFunction（第119行）
- **测试**：test_new_features.py TestCurrying（5个测试）

### 4.6 函数组合 ✅

- **实现位置**：`builtins/hof.py` _hof_组合()、_hof_管道组合()
- **测试**：test_new_features.py TestFunctionComposition（5个测试）

### 4.7 模式匹配 ✅

- **基础匹配**（值/通配符）、**列表解构匹配**、**字典解构匹配**、**守卫条件** `当`
- **枚举匹配**：`_match_pattern()` 中处理 `EnumVariantPattern` 和 `FunctionCall+MemberAccess` 形式的枚举变体模式
- **类型检查模式**：`_match_pattern()` 中处理 `TypeCheckPattern`
- **实现位置**：`interpreter/statements.py` exec_match()、_match_pattern()；`parser/modules/pattern_matching.py`
- **测试**：test_pattern_matching.py；test_new_features.py TestPatternMatching, TestEnumPatternMatching, TestTypeCheckPattern

### 4.8 惰性求值 ✅

- **`产出` 关键字和生成器函数**：重构 `DaoGenerator` 为真正的惰性求值模式（使用 Python 生成器实现按需产出）
- **`流`(Stream) 惰性数据结构**：`DaoStream` 类，按需计算，支持链式操作
- **`映射流` 内置函数**：对流惰性映射
- **`筛选流` 内置函数**：对流惰性筛选
- **`取` 内置函数**：从流中取前 N 个值
- **实现位置**：`builtins/oop_types.py` DaoGenerator, DaoStream；`builtins/hof.py` _hof_映射流, _hof_筛选流, _hof_取
- **测试**：test_generator.py；test_new_features.py TestGenerators, TestLazyStream

### 4.9 不可变数据操作 ✅

- **模式匹配中的展开** `...`（收集剩余元素语义）
- **解构赋值中的 rest**
- **字典字面量中的展开**：`{...dict1, "新键": 值}` — 修改 `parse_dict_literal()` 和 `parse_set_or_block_literal()`，解释器中处理 `SpreadExpr`
- **列表字面量中的展开**：`[...list1, 新元素]` — 修改 `parse_list_literal()`，解释器中处理 `SpreadExpr`
- **实现位置**：`ast_nodes.py` SpreadExpr；`parser/expressions.py` parse_list_literal(), parse_dict_literal(), parse_set_or_block_literal()；`interpreter/expressions.py` ListLiteral/DictLiteral 求值
- **测试**：test_interpreter.py TestSpreadOperator（6个测试）
