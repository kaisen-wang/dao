// 流操作示例：数据处理管道
// ============================
// 这个示例展示了如何使用高阶函数和管道运算符
// 来构建优雅的数据处理管道。

// 示例 1：基础映射操作
定义 numbers = [1, 2, 3, 4, 5]
定义 doubled = 映射(numbers, 函数(x) => x * 2)
打印("原始数据: " + numbers)
打印("映射后: " + doubled)
打印

// 示例 2：筛选操作
定义 evens = 筛选(numbers, 函数(x) => x % 2 == 0)
定义 odds = 筛选(numbers, 函数(x) => x % 2 != 0)
打印("偶数: " + evens)
打印("奇数: " + odds)
打印

// 示例 3：折叠（归约）操作
定义 sum = 折叠(numbers, 函数(acc, x) => acc + x, 0)
定义 product = 折叠(numbers, 函数(acc, x) => acc * x, 1)
打印("总和: " + sum)
打印("乘积: " + product)
打印

// 示例 4：排序操作
定义 unsorted = [3, 1, 4, 2, 5]
定义 sorted_nums = 排序(unsorted)
定义 sorted_desc = 排序(unsorted, 降序为真)
打印("升序排序: " + sorted_nums)
打印("降序排序: " + sorted_desc)
打印

// 示例 5：条件检查
定义 all_positive = 每个满足(numbers, 函数(x) => x > 0)
定义 has_even = 存在满足(numbers, 函数(x) => x % 2 == 0)
打印("所有数字都是正数: " + all_positive)
打印("存在偶数: " + has_even)
打印

// 示例 6：查找操作
定义 found = 查找(numbers, 函数(x) => x > 3)
打印("第一个大于3的数: " + found)
打印

// 示例 7：展平映射（处理嵌套列表）
定义 nested = [[1, 2], [3, 4], [5, 6]]
定义 flattened = 展平映射(nested, 函数(arr) => 映射(arr, 函数(x) => x * 2))
打印("展平并映射: " + flattened)
打印

// 示例 8：函数柯里化
定义 add = 函数(a, b) => a + b
定义 add_five = 柯里化(add, 5)
定义 result1 = add_five(10)
打印("柯里化结果: " + result1)
打印

// 示例 9：函数组合
定义 double = 函数(x) => x * 2
定义 add_ten = 函数(x) => x + 10
定义 composed = 组合(add_ten, double)
定义 result2 = composed(5)
打印("组合结果: " + result2)
打印

// 示例 10：反转列表
定义 reversed_list = 反转(numbers)
打印("反转列表: " + reversed_list)
