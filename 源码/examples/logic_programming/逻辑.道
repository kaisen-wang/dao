// 定义一个逻辑知识库
逻辑 家庭关系
    // 定义事实
    事实: 父母("张三", "小明")
    事实: 父母("李四", "小明")
    事实: 父母("张三", "小红")
    事实: 父母("小明", "乐乐")

    // 定义规则
    规则: 祖父母(?祖, ?孙) 如果
        父母(?祖, ?父) 并且 父母(?父, ?孙)

    规则: 兄弟姐妹(?甲, ?乙) 如果
        父母(?父, ?甲) 并且 父母(?父, ?乙) 并且 ?甲 != ?乙

// 在普通代码中进行查询
定义 结果 = 查询(家庭关系, 祖父母("张三", ?孙))

// 查询会返回一个包含所有可能解的列表
遍历 每个解 在 结果
    打印(`张三的孙辈是: {每个解.孙}`)
// 输出：张三的孙辈是: 乐乐

// 查询兄弟姐妹
定义 兄妹 = 查询(家庭关系, 兄弟姐妹("小明", ?谁))
遍历 每个解 在 兄妹
    打印(`小明的兄弟姐妹: {每个解.谁}`)
// 输出：小明的兄弟姐妹: 小红
