// 数独求解示例
// 展示约束逻辑编程在组合问题求解中的应用

// 简化版数独：4x4 网格
// 规则：每行、每列、每个2x2宫格内数字1-4不重复

逻辑 数独求解
    // 约束：每个格子的值在1-4范围内
    规则: 有效行(?a, ?b, ?c, ?d) 如果
        ?a 在范围 1..4 并且
        ?b 在范围 1..4 并且
        ?c 在范围 1..4 并且
        ?d 在范围 1..4 并且
        ?a != ?b 并且 ?a != ?c 并且 ?a != ?d 并且
        ?b != ?c 并且 ?b != ?d 并且
        ?c != ?d

    // 约束：列不重复
    规则: 有效列(?a1, ?a2, ?a3, ?a4) 如果
        ?a1 != ?a2 并且 ?a1 != ?a3 并且 ?a1 != ?a4 并且
        ?a2 != ?a3 并且 ?a2 != ?a4 并且
        ?a3 != ?a4

// 数学谜题示例：找出满足条件的数字组合
逻辑 数学谜题
    // 约束：找出满足 x+y=15 且 x*y>50 的整数对
    规则: 谜题(?x, ?y) 如果
        ?x 在范围 1..10 并且
        ?y 在范围 1..10 并且
        ?x + ?y == 15 并且
        ?x * ?y > 50

// 求解数学谜题
定义 解集 = 查询(数学谜题, 谜题(?x, ?y))
遍历 解 在 解集
    打印(`x={解.x}, y={解.y}`)

// 预期输出：
// x=6, y=9
// x=7, y=8
// x=8, y=7
// x=9, y=6
