// 调度问题示例
// 展示约束逻辑编程在任务调度中的应用

// 场景：3个任务分配给2个工人
// 约束：每个任务只能分配给一个工人，同一工人不能同时执行重叠时段的任务

逻辑 任务调度
    // 定义任务及其时段
    事实: 任务("任务A", 1, 3)   // 任务A：时段1-3
    事实: 任务("任务B", 2, 4)   // 任务B：时段2-4
    事实: 任务("任务C", 4, 6)   // 任务C：时段4-6

    // 定义工人
    事实: 工人("甲")
    事实: 工人("乙")

    // 规则：有效的任务分配
    // 每个任务分配给一个工人
    规则: 分配(?任务, ?工人) 如果
        任务(?任务, ?开始, ?结束) 并且
        工人(?工人)

// 查询所有可能的分配方案
定义 分配方案 = 查询(任务调度, 分配(?任务, ?工人))
遍历 方案 在 分配方案
    打印(`{方案.任务} -> {方案.工人}`)

// 数学约束示例：资源分配
逻辑 资源分配
    // 约束：总预算100，项目A花费x，项目B花费y
    // x + y <= 100，x >= 20，y >= 30
    规则: 可行分配(?x, ?y) 如果
        ?x 在范围 20..70 并且
        ?y 在范围 30..80 并且
        ?x + ?y <= 100

// 求解资源分配
定义 分配解 = 查询(资源分配, 可行分配(?x, ?y))
打印("可行的资源分配方案数: " + 分配解.长度)
遍历 解 在 分配解
    打印(`项目A: {解.x}, 项目B: {解.y}, 总计: {解.x + 解.y}`)
