// 道语言 - 基础语法示例

// ===== 变量与常量 =====
定义 名字 = "张三"
常量 年龄 = 25
打印(名字)
打印(年龄)

// ===== 条件判断 =====
如果 年龄 >= 18
    打印("成年人")
否则
    打印("未成年人")

// ===== 循环 =====
遍历 i 从 1 到 5
    打印(i)

定义 水果 = ["苹果", "香蕉", "橙子"]
遍历 每个 在 水果
    打印(每个)

// ===== 函数 =====
函数 加法(甲, 乙)
    返回 甲 + 乙

定义 结果 = 加法(3, 4)
打印(结果)

// ===== 错误处理 =====
尝试
    定义 x = 10 / 0
捕获 错误
    打印("捕获到错误！")
最终
    打印("程序结束")
