// 道语言 - 枚举类型示例
// 演示：枚举定义、枚举值访问

// ===== 定义枚举 =====
枚举 颜色 { 红, 绿, 蓝, 黄 }

枚举 方向 { 北, 南, 东, 西 }

枚举 状态 { 空闲, 运行中, 停止, 错误 }

// ===== 使用枚举 =====
打印("=== 枚举类型示例 ===")

// 获取枚举对象
打印(颜色)
打印(方向)

// TODO: 添加枚举值访问功能（如 颜色.红，方向.北等）
// 这需要在解释器中添加枚举成员访问支持

打印("\n枚举类型演示完成！")
