异步 函数 主程序()
    打印("=== 极简单通道测试 ===")

    定义 ch = 通道()

    // 发送任务 - 立即发送
    并行
        发送 ch "Hello"

    // 接收任务 - 立即接收
    定义 结果 = 接收 ch
    打印("收到:", 结果)

    // 缓冲通道测试
    打印("\n=== 缓冲通道 ===")
    定义 bc = 通道(1)

    发送 bc "World"
    打印("发送成功")

    定义 r = 接收 bc
    打印("收到:", r)

    打印("\n✅ 所有测试通过!")

等待 主程序()
