异步 函数 异步加法(x, y)
    睡眠(0.5)
    返回 x + y

异步 函数 异步乘法(x, y)
    睡眠(0.8)
    返回 x * y

异步 函数 主()
    定义 a = 等待 异步加法(2, 3)
    打印("异步加法结果:", a)

    定义 b = 等待 异步乘法(4, 5)
    打印("异步乘法结果:", b)

运行异步(主())
