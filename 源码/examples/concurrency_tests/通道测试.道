异步 函数 主程序()
    打印("通道测试")

    定义 ch1 = 通道()
    打印("无缓冲通道创建成功")

    定义 ch2 = 通道(5)
    打印("缓冲通道(5)创建成功")

    并行
        发送 ch1 "消息1"
    并行
        打印("收到:", 接收 ch1)

    发送 ch2 "消息2"
    打印("缓冲通道发送成功")
    打印("缓冲通道收到:", 接收 ch2)

    打印("所有测试完成")

等待 主程序()
