// 抽象类型示例：形状类层次结构
// ================================
// 这个示例展示了如何使用 抽象 类型 来定义抽象基类，
// 然后由具体类实现抽象方法。

// 定义抽象类：形状
抽象 类型 形状
    // 抽象方法：计算面积
    抽象 获取面积()
    // 抽象方法：计算周长
    抽象 获取周长()
    // 具体方法：检查是否为正方形
    函数 是正方形()
        返回 假

// 具体类：圆形
类型 圆形 继承自 形状
    初始化(半径)
        本对象.半径 = 半径
    函数 获取面积()
        返回 3.14159 * 本对象.半径 * 本对象.半径
    函数 获取周长()
        返回 2 * 3.14159 * 本对象.半径

// 具体类：矩形
类型 矩形 继承自 形状
    初始化(宽, 高)
        本对象.宽 = 宽
        本对象.高 = 高
    函数 获取面积()
        返回 本对象.宽 * 本对象.高
    函数 获取周长()
        返回 2 * (本对象.宽 + 本对象.高)
    函数 是正方形()
        返回 本对象.宽 == 本对象.高

// 具体类：正方形
类型 正方形 继承自 矩形
    初始化(边长)
        父对象.初始化(边长, 边长)

// 测试
定义 circle = 圆形(5)
定义 rectangle = 矩形(4, 6)
定义 square = 正方形(3)

打印("圆形面积: " + circle.获取面积())
打印("圆形周长: " + circle.获取周长())
打印("矩形面积: " + rectangle.获取面积())
打印("矩形周长: " + rectangle.获取周长())
打印("矩形是正方形: " + rectangle.是正方形())
打印("正方形面积: " + square.获取面积())
打印("正方形周长: " + square.获取周长())
打印("正方形是正方形: " + square.是正方形())

// 尝试实例化抽象类（会报错）
// 定义 shape = 形状()  // 错误：抽象类不能被实例化
