异步 函数 延迟执行(毫秒)
    打印("开始延迟执行", 毫秒, "毫秒")
    等待 睡眠(毫秒 / 1000)
    打印("延迟执行完成")
    返回 毫秒 * 2

异步 函数 主程序()
    打印("=== 异步函数基础测试 ===")

    定义 结果 = 等待 延迟执行(500)
    打印("结果:", 结果)

    打印("✅ 异步基础功能正常!")

等待 主程序()
