异步 函数 主程序()
    打印("测试选择语句支持'当'关键字")

    打印("\n测试1: 通道通信")
    定义 ch = 通道()

    并行
        发送 ch "数据1"

    选择 {
        情况 接收 ch 作为 消息:
            打印("收到消息:", 消息)
        情况 超时(2.0):
            打印("超时")
    }

    打印("\n✅ 选择语句功能测试通过")

等待 主程序()
