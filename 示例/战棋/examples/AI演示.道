/// AI 演示 — AI 自动对战 + 通道对战对比
/// 用法: dao AI演示.道

从 核心.日志 导入 创建日志器
从 并发.网络对战 导入 AIvsAI, 通道对战

导出 函数 主()
    定义 日志 = 创建日志器("AI演示")
    日志.信息("=== AI 对战演示 ===")

    // 同步对战：每回合 AI 决策并立即执行
    定义 同步结果 = AIvsAI("勇者", "法师")
    日志.信息(`同步对战获胜者: {同步结果}`)

    // 通道对战：通过通道通信和超时选择驱动
    定义 通道结果 = 通道对战("勇者", "法师", 15)
    日志.信息(`通道对战获胜者: {通道结果}`)

    日志.信息("=== AI 演示结束 ===")

主()
