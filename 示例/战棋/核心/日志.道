// 道语言 - 日志系统

导出 常量 级别调试 = 0
导出 常量 级别信息 = 1
导出 常量 级别警告 = 2
导出 常量 级别错误 = 3

导出 类型 日志器
    初始化(名称, 级别=级别调试)
        本对象.名称 = 名称
        本对象.级别 = 级别

    函数 记录(级别值, 消息)
        如果 级别值 >= 本对象.级别
            打印(`[{级别值}][{本对象.名称}] {消息}`)

    函数 调试(消息)
        本对象.记录(级别调试, 消息)

    函数 信息(消息)
        本对象.记录(级别信息, 消息)

    函数 警告(消息)
        本对象.记录(级别警告, 消息)

    函数 错误(消息)
        本对象.记录(级别错误, 消息)

导出 函数 创建日志器(名称)
    返回 日志器(名称)
