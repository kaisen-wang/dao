// 道语言 - 可装备接口与装备类型

从 实体.卡牌 导入 稀有度

导出 特征 可装备
    抽象 装备(角色)
    抽象 卸下(角色)
    抽象 是否装备中()

导出 枚举 装备槽位
    武器
    防具
    饰品
    特殊

导出 类型 装备
    实现 可装备
    初始化(名称, 槽位, 稀有度=稀有度.普通)
        本对象.名称 = 名称
        本对象.槽位 = 槽位
        本对象.稀有度 = 稀有度
        本对象.装备中 = 假

    函数 装备(角色)
        本对象.装备中 = 真
        角色.装备槽[本对象.槽位] = 本对象
        本对象.应用效果(角色)

    函数 卸下(角色)
        本对象.装备中 = 假
        本对象.移除效果(角色)
        角色.装备槽[本对象.槽位] = 空

    函数 是否装备中()
        返回 本对象.装备中

    函数 应用效果(角色)
        空

    函数 移除效果(角色)
        空

    函数 描述()
        返回 `{本对象.名称}[{本对象.槽位}]({本对象.稀有度})`

导出 类型 武器 继承自 装备
    初始化(名称, 攻击力, 稀有度=稀有度.普通)
        本对象.攻击力 = 攻击力
        父对象.初始化(名称, 装备槽位.武器, 稀有度)

    函数 应用效果(角色)
        角色.攻击力 = 角色.攻击力 + 本对象.攻击力

    函数 移除效果(角色)
        角色.攻击力 = 角色.攻击力 - 本对象.攻击力

    函数 描述()
        返回 `{父对象.描述()} 攻击+{本对象.攻击力}`

导出 类型 防具 继承自 装备
    初始化(名称, 防御力, 稀有度=稀有度.普通)
        本对象.防御力 = 防御力
        父对象.初始化(名称, 装备槽位.防具, 稀有度)

    函数 应用效果(角色)
        角色.防御力 = 角色.防御力 + 本对象.防御力

    函数 移除效果(角色)
        角色.防御力 = 角色.防御力 - 本对象.防御力

    函数 描述()
        返回 `{父对象.描述()} 防御+{本对象.防御力}`
