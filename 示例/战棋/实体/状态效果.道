// 道语言 - 状态效果（Buff/Debuff）

导出 枚举 效果类型
    增益
    减益
    控制
    持续伤害
    持续治疗

导出 特征 效果
    抽象 名称()
    抽象 描述()
    抽象 回合数()
    抽象 应用(目标)
    抽象 移除(目标)

导出 类型 燃烧
    实现 效果
    初始化(回合)
        本对象.剩余回合 = 回合

    函数 名称()
        返回 "燃烧"

    函数 描述()
        返回 `每回合受到3点伤害，持续{本对象.剩余回合}回合`

    函数 回合数()
        返回 本对象.剩余回合

    函数 应用(目标)
        目标.生命值 = 目标.生命值 - 3
        本对象.剩余回合 = 本对象.剩余回合 - 1
        `{目标.名称}受到3点燃烧伤害`

    函数 移除(目标)
        空

导出 类型 冰冻
    实现 效果
    初始化(回合)
        本对象.剩余回合 = 回合

    函数 名称()
        返回 "冰冻"

    函数 描述()
        返回 `无法行动，持续{本对象.剩余回合}回合`

    函数 回合数()
        返回 本对象.剩余回合

    函数 应用(目标)
        目标.冻结 = 真
        本对象.剩余回合 = 本对象.剩余回合 - 1

    函数 移除(目标)
        目标.冻结 = 假

导出 类型 力量加成
    实现 效果
    初始化(回合, 加成值)
        本对象.剩余回合 = 回合
        本对象.加成值 = 加成值

    函数 名称()
        返回 "力量加成"

    函数 描述()
        返回 `攻击力+{本对象.加成值}，持续{本对象.剩余回合}回合`

    函数 回合数()
        返回 本对象.剩余回合

    函数 应用(目标)
        目标.力量 = 目标.力量 + 本对象.加成值
        本对象.剩余回合 = 本对象.剩余回合 - 1

    函数 移除(目标)
        目标.力量 = 目标.力量 - 本对象.加成值

导出 类型 护盾
    实现 效果
    初始化(回合, 护盾值)
        本对象.剩余回合 = 回合
        本对象.护盾值 = 护盾值

    函数 名称()
        返回 "护盾"

    函数 描述()
        返回 `减伤{本对象.护盾值}点，持续{本对象.剩余回合}回合`

    函数 回合数()
        返回 本对象.剩余回合

    函数 应用(目标)
        目标.护甲值 = 目标.护甲值 + 本对象.护盾值
        本对象.剩余回合 = 本对象.剩余回合 - 1

    函数 移除(目标)
        目标.护甲值 = 最大值(0, 目标.护甲值 - 本对象.护盾值)
